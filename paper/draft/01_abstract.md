# Abstract

This project presents a full soft-computing benchmark for epileptic seizure recognition using a staged Cartesian workflow. The pipeline evaluates preprocessing, feature reduction, feature selection, and classification under one reproducible protocol with two tracks (binary and multiclass). The benchmark covers 1536 unique method combinations and 4608 fold evaluations with robust skip/failure logging for invalid combinations.
